// 生成start节点的输出配置

export function generateStartNodeOutputs() {
  // 这个函数已废弃，输出配置将在form-meta.tsx中直接生成
  console.warn('[generateStartNodeOutputs] 此函数已废弃，请在React组件中直接使用hooks');

  return {
    type: 'object',
    properties: {},
  };
}
